# TypeScript Standards

- TypeScript strict mode stays enabled.
- Next.js App Router is the default frontend structure.
- Feature code belongs under `features`.
- Reusable visual components belong under `components`.
- Zustand stores belong under `lib/store` or feature-local state folders.
- TanStack Query setup belongs under `lib/query`.

