# Git Standards

- Keep commits scoped to one module or one cross-cutting concern.
- Update module boundary docs when adding dependencies.
- Add migrations in the same change as the code that requires them.
- Do not commit local `.env` files.

