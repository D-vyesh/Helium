# Testing Standards

- Unit tests live with the module they verify.
- Cross-module tests live under `testing/integration`.
- Ledger reconciliation tests live under `testing/ledger-reconciliation`.
- No production credentials are allowed in fixtures.

