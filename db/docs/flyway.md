# Flyway Setup

Runtime Flyway migrations are loaded from:

```text
backend/helium-core/app/src/main/resources/db/migration
```

The MVP starts with no database tables. Add migrations only when a domain implementation begins.

